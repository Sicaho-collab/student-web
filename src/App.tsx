// App.tsx is intentionally minimal — routing is handled by src/routes/index.tsx.
// RouterProvider in main.tsx directly renders the route tree.
// This file is kept as a placeholder for future app-level providers
// (e.g., ThemeProvider, QueryClientProvider).

export default function App() {
  return null
}
